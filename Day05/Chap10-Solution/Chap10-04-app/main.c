#include<stdio.h>

int main(void) {
	int a = 10;
	int b = a++ + 5;

	printf("a: %d, b: %d\n", a, b);


// 아래코드에서 a, b의  값은 어떻게 출려되나? 
// 7번라인의 주석형식으로 설명하시오.
// // a++는 '후위 증가 연산자'로, 현재 a 값(10)을 먼저 사용해 b = 10 + 5 = 15가 되고,
	// 그 다음에 a의 값이 1 증가하여 a는 11이 된다.

	int x = 7;
	
	if (x % 2 == 0)
		printf("A");
	else if (x > 5)
		printf("B");
	else
		printf("C");

	return 0;
}

//	아래의 코드가 어떻게 동작하고 그 결과에 대한 설명을 기술하세요 
// 아래의 코드는 if–else if–else 조건문을 사용하여 변수 x의 값에 따라
// 서로 다른 문자를 출력한다.
// 먼저 x % 2 == 0 조건을 검사하는데, x가 7이므로 2로 나눈 나머지는 1이 되어
// 조건은 거짓이 된다.
// 따라서 첫 번째 if문은 실행되지 않고 else if 조건인 x > 5를 검사한다.
// x는 7이므로 5보다 커서 해당 조건은 참이 되어 "B"가 출력된다.
// else if 조건이 참이므로 else 문은 실행되지 않는다.
// 최종 출력 결과는 B이다.